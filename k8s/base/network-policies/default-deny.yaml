apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: three-tier-app
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  # No rules = deny all (Flannel supports this basic deny)